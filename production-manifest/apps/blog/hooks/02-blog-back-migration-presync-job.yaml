apiVersion: batch/v1
kind: Job
metadata:
  name: blog-back-migration-presync
  namespace: blog-production
  labels:
    app.kubernetes.io/name: blog-back-migration
    app.kubernetes.io/part-of: blog-back
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/sync-wave: "-1"
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
spec:
  backoffLimit: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: blog-back-migration
        app.kubernetes.io/part-of: blog-back
    spec:
      restartPolicy: Never
      nodeSelector:
        kubernetes.io/hostname: rke2-worker-1
      imagePullSecrets:
        - name: kuberneteshub
      containers:
        - name: migration
          image: kareemdev2/blog-back-fpm:prod-85
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: blog-back-secret
          volumeMounts:
            - name: env-file
              mountPath: /app/.env
              subPath: .env
              readOnly: true
          command:
            - php
            - bin/console
            - doctrine:migrations:migrate
            - --no-interaction
            - --env=preprod
      volumes:
        - name: env-file
          secret:
            secretName: blog-back-env-file
